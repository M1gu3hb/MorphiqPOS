# Veredicto del verificador — Mesas y comandas (E6-2, E6-4, E6-6, E6-8) · packages/app/src/restaurante + apps/web/app/api/restaurante

VEREDICTO: NO_CUMPLE

## Puertas que ejecutó él mismo
- typecheck: VERDE — `pnpm --filter @morphiqpos/app exec tsc --noEmit` → exit 0 (ejecutado por mí, sin salida).
- eslint: VERDE — `pnpm exec eslint packages/app/src/restaurante apps/web/app/api/restaurante` → exit 0. Única línea impresa: el aviso `Pages directory cannot be found...` del plugin de Next, que no es un hallazgo. Confirmo la afirmación del informe.
- pruebas: VERDE PERO ENGAÑOSO — `pnpm vitest run packages/app/src/restaurante` → 48/48 en 1.06s. Suite completa `pnpm vitest run packages/app` → 25 archivos, 357 pruebas (el informe dice 355). Las dos pruebas clave NO son tautologías: verificado por mutación (ver incumplimiento 3 para lo que sí falta). Cobertura real medida con --coverage: 17.04% stmts / 17.08% branch / 13.91% funcs / 16.23% lines.

## Incumplimientos

### 1. [BLOQUEANTE] packages/app/src/restaurante/pedido.ts:207

**Regla:** El flujo mesa → cocina → caja tiene que poder completarse. Una orden viva no puede quedar sin ninguna transición posible (R14: los estados cambian por transición declarada).

**Escenario:** El mesero abre la mesa 5, captura tres platos y pulsa «Enviar a cocina». `confirmarOrden` (pedido.ts:203-223) escribe `ordenes.estado='confirmada'`. A partir de ese instante la orden NO TIENE NINGUNA SALIDA: (a) el cajero pulsa Cobrar y recibe «Esa venta ya se cobró» — `venta/cobrar.ts:68` exige `estado==='borrador'`, y `packages/data/src/repos/ordenes/cierre.ts:91` lleva `.where('estado','=','borrador')`; (b) el mesero intenta añadir un postre y recibe ORDEN_NO_EDITABLE — `venta/carrito.ts:229` exige lo mismo; (c) el mesero pulsa «Liberar mesa» y recibe MESA_NO_LIBERABLE — `restaurante/mesas.ts:148-161`, porque la orden tiene líneas y su estado no está en ORDEN_YA_CERRADA=['pagada','cancelada']; (d) no existe NINGÚN comando de cancelación en todo `packages/app` (grep de `estado: 'cancelada'` devuelve un único resultado: `mesas-escrituras.ts:138`, dentro de `cancelarOrdenVacia`, que sólo corre cuando la orden NO tiene líneas). En la base queda: fila de `ordenes` en 'confirmada', con `orden_lineas`, sin folio, sin `cerrada_en`; `mesas.orden_activa_id` clavado y `mesas.estado='pedido_enviado'`; y el índice único `ordenes_una_activa_por_mesa` (046_restricciones_restaurante.sql:64-67, que incluye 'confirmada') impide abrir cualquier orden nueva en esa mesa. La mesa 5 queda FUERA DE SERVICIO hasta que alguien ejecute SQL a mano. El informe declara la mitad (a) y omite (b), (c) y (d).

### 2. [BLOQUEANTE] packages/app/src/restaurante/cuenta.ts:72

**Regla:** Un comando de restaurante sólo puede operar sobre órdenes de restaurante. Ningún comando puede sacar de 'borrador' una orden de mostrador ajena a su flujo.

**Escenario:** `solicitarCuenta` carga la orden con `ordenDeMesa` (datos.ts:94-122), que filtra SÓLO por `organizacion_id` e `id`: no mira `estrategia_captura`, ni `estrategia_cumplimiento`, ni `mesa_id`. Su única guarda es `ORDEN_CERRADA=['pagada','cancelada']` (cuenta.ts:72 y el `.where('estado','not in',...)` de cuenta.ts:183) — es decir, acepta 'borrador'. Escenario: el cajero tiene un carrito de mostrador abierto (`ordenes` en 'borrador', `mesa_id` NULL, `terminal_id` puesto por `ordenes_carrito_por_terminal`, 003_venta_caja_inventario.sql:185). Alguien con rol `cajero` —que está en ROLES_DE_SALA (cuenta.ts:28)— pega o teclea ESE `ordenId` en POST /api/restaurante/solicitar-cuenta (id equivocado, tablet en la cuenta que no era, cliente que reintenta con un id viejo). El comando: pone `estado='cuenta_solicitada'`, PISA subtotal/descuento/impuestos/total/costo/utilidad/margen_bp con lo que devuelve `cotizar`, y estampa `codigo_caja='M00-####'` (mesa null → `mesa?.numero ?? 0`, cuenta.ts:95, más `generarCodigoCaja`, cuenta.ts:224-228). Resultado en la base: la venta de mostrador ya no es cobrable (`cobrar.ts:68`) ni editable (`carrito.ts:229`) ni cancelable (no hay comando), y arrastra un `codigo_caja` de una mesa que no existe. Lo mismo por `enviar_pedido` (pedido.ts:81, ORDEN_ADMITE_PEDIDO incluye 'borrador'): además inserta comandas de cocina para una venta de mostrador, porque `comandas.mesa_id` es NULLABLE (045_restaurante.sql:302) y no hay nada que lo impida.

### 3. [GRAVE] packages/app/src/restaurante/restaurante.test.ts:1

**Regla:** Cobertura mínima de pruebas 80% (common/testing.md). Las pruebas tienen que ejercitar el código que escribe dinero y estados.

**Escenario:** Medido por mí: `pnpm vitest run packages/app/src/restaurante --coverage --coverage.include='packages/app/src/restaurante/**'` → All files 17.04% stmts, 13.91% funcs, 16.23% lines. Los SEIS comandos entregados están al 0%: mesas.ts 0% (35-180), pedido.ts 0% (43-241), preparacion.ts 0% (30-249), cuenta.ts 0% (28-234). También al 0%: lineas.ts (donde se arma `filaDeLinea` con `utilidad_centavos: total - costoTotal`, el invariante de F1-04 §7.3), comandas.ts, items.ts, propagacion.ts, datos.ts, mesas-escrituras.ts, preparacion-escrituras.ts. Las 48 pruebas sólo tocan estaciones.ts (135 líneas), transiciones.ts (195) y la FORMA de esquemas.ts. Consecuencia concreta: ninguno de los dos BLOQUEANTES de arriba puede ser detectado nunca por esta suite, porque ninguna prueba llega a ejecutar una línea de los comandos. Además esquemas.ts:26 (el `.refine(BigInt(...) > 0n)` de la cantidad) figura como NO CUBIERTO: no existe ninguna prueba que afirme que `cantidad: '0'` o `'0.0000'` se rechaza. El informe presenta «48 pruebas» sin declarar que la cobertura de los comandos es cero.

### 4. [MENOR] packages/app/src/restaurante/pedido.ts:126

**Regla:** El estado de la mesa tiene que describir un hecho real del salón.

**Escenario:** `avanzarMesa` se llama SIEMPRE que `orden.mesaId !== null` (pedido.ts:124-127), pero las comandas sólo se escriben si `comandas.length > 0` (pedido.ts:102). La mesa 7 pide dos refrescos de botella, cuyo producto tiene `area_preparacion='ninguno'`: `areasDe` devuelve `[]` (estaciones.ts:37), `agruparEnComandas` devuelve cero grupos, no se inserta ninguna comanda — y aun así `mesas.estado` pasa a 'pedido_enviado'. En la base queda una mesa anunciando «pedido enviado» sin una sola fila en `comandas`. Ninguna pantalla de cocina la verá nunca, así que ningún evento de cocina la sacará de ahí: sólo `solicitar_cuenta` o `entregar_pedidos` la mueven después.

### 5. [MENOR] packages/app/src/restaurante/preparacion.ts:230

**Regla:** Un comando que no hizo nada no debe escribir estado.

**Escenario:** En `entregarPedidos`, `sincronizarMesa` se invoca con `estadoComanda:'entregado'` incluso cuando `ids.length === 0` (el bloque que hace trabajo real está guardado en preparacion.ts:218, la sincronía no). Combinado con el caso anterior: mesa 7 en 'pedido_enviado' sin ninguna comanda; el mesero pulsa «entregar»; `listas` sale vacía, `quedanComandasActivas` devuelve false, y `mesaTrasComanda(estado,'entregado',false)` devuelve 'ocupada' (transiciones.ts:181). Se escribe `mesas.estado='ocupada'` y se audita una entrega que no entregó nada.

### 6. [MENOR] packages/app/src/restaurante/mesas.ts:149

**Regla:** Cada escritura y cada lectura decisiva dentro de la transacción va envuelta en `ctx.paso` (definicion.ts:45-52: «Marca un paso con nombre, para poder interrumpirlo desde una prueba»).

**Escenario:** `const consumida = await tieneLineas(ctx.tx, ...)` va sin `ctx.paso`, y lo mismo `await mesaOperable(...)` en cuenta.ts:89. La atomicidad NO está rota (ambas usan `ctx.tx`), pero son invisibles al arnés de inyección de fallos por nombre de paso: una prueba futura que quiera interrumpir exactamente la lectura que decide MESA_NO_LIBERABLE no tiene un nombre al que agarrarse, y como los pasos se identifican por nombre y no por índice, el hueco no se nota.

### 7. [MENOR] packages/app/src/restaurante/restaurante.test.ts:303

**Regla:** El detector de claves de ámbito tiene que cubrir las variantes, no sólo el nombre exacto.

**Escenario:** `PARECE_AMBITO` está anclado con `^...$` y `definicion.ts:23-36` compara contra un Set de nombres exactos. Una clave futura llamada `posRol`, `usuarioRol`, `rolSolicitado` o `usuario` pasaría por los DOS guardas sin saltar nada — ni la prueba, ni el `throw` al cargar el módulo. Hoy ningún esquema declara una (lo verifiqué recorriendo los seis), así que es una debilidad latente, no un incumplimiento consumado; pero la garantía que el informe vende («es un módulo que no arranca si se incumple», esquemas.ts:11-13) sólo vale para la ortografía exacta de la lista.

## Archivos fuera de sitio
- packages/app/src/venta/cobrar.ts — modificado (M), pero NO por este agente: el diff (+68 líneas) es íntegramente el módulo de propinas (`marcarPropinaDeOrden`, `registrarPagoConPropina`, `propinaEfectivo`). La guarda `if (orden.estado !== 'borrador')` de la línea 68 sigue INTACTA, así que el bloqueante 1 está vivo en el árbol de trabajo actual.
- packages/app/src/venta/pagos.ts — modificado (M), +41 líneas, también del agente de propinas. Fuera del alcance de este módulo pero dentro de la excepción declarada (`packages/app/src/venta` sólo para propinas).
- packages/app/src/demostracion/datos.ts, packages/app/src/demostracion/resetear.ts, packages/app/src/demostracion/sala.ts — aparecieron en `git status` ENTRE mis dos ejecuciones del comando (no estaban en la primera). Son de un agente concurrente, no de este módulo.
- NOTA DE ATRIBUCIÓN: en mi primera lectura de `git status --short` figuraban también `packages/app/src/puente/cobertura.test.ts` y `packages/app/src/puente/mapa.ts` como modificados; en la segunda ya no. Hay agentes escribiendo en paralelo, así que este listado es una foto de un instante.
- VEREDICTO DE ALCANCE: no hay ni un solo archivo modificado que sea atribuible al agente de restaurante fuera de `packages/app/src/restaurante/` y `apps/web/app/api/restaurante/`, ambos untracked (??) y por tanto nuevos. Ese punto del encargo SÍ se cumple.
- NOTA DE MI PROPIA INTERVENCIÓN: para verificar que las pruebas no son tautologías muté temporalmente esquemas.ts y transiciones.ts, ejecuté vitest (2 pruebas murieron, como debían) y restauré desde copia. md5 posterior idéntico al previo: esquemas.ts cbdd204252a193b3f143797884de7eea, transiciones.ts 6656670a43140cabf3443520c58f5a4a. El árbol queda como lo encontré.

## Resumen del verificador

NO CUMPLE. Las tres puertas están verdes de verdad —las ejecuté yo: tsc exit 0, eslint exit 0 (el aviso de «Pages directory» es del plugin de Next, no un hallazgo), 48/48 pruebas, y la suite completa 25 archivos / 357 pruebas (el informe dice 355)— y buena parte de lo que el informe presume es cierto: ningún esquema acepta importe, precio ni rol (lo verifiqué por MUTACIÓN: añadir `precioUnitarioCentavos` dentro de `lineas` mata la prueba); la autorización sale de `ctx.ambito.rol` en comando.ts:98 y `definirComando` rechaza al cargar cualquier clave de ámbito; no hay un solo catch vacío (el único `catch`, mesas-escrituras.ts:58, re-lanza en :68); no hay `any`, `as unknown as`, `@ts-ignore` ni `eslint-disable`; el dinero es bigint en todo el módulo (cero `Math.round`, cero `parseFloat`, cero `*100`; `aplicarPorcentaje` toma bigint y puntos base enteros); todo `await` va sobre `ctx.tx`; y las 24 consultas filtran por `organizacion_id`, incluidos los tres predicados del SQL crudo de propagacion.ts, que además parametriza los ids con `sql.val`. También maté un segundo mutante (`listo → en_preparacion` permitido) para confirmar que la tabla de transiciones se prueba de verdad. Pero el módulo no se puede aprobar por tres razones. (1) El bloqueante que el autor declara es PEOR de lo que declara: tras `enviar_pedido` la orden no sólo deja de ser cobrable (cobrar.ts:68, cierre.ts:91) — también deja de ser editable (carrito.ts:229), la mesa deja de ser liberable (mesas.ts:154) y NO EXISTE ningún comando de cancelación en todo packages/app que acepte una orden con líneas, así que la mesa queda fuera de servicio de forma permanente y sólo se rescata con SQL a mano. (2) Un bloqueante que el informe NO declara: ni `enviar_pedido` ni `solicitar_cuenta` comprueban `estrategia_captura` ni `mesa_id`, y `ordenDeMesa` devuelve cualquier orden de la organización — de modo que enviar el id de un carrito de MOSTRADOR a /api/restaurante/solicitar-cuenta lo saca de 'borrador', le pisa los totales y lo deja igual de inservible, con un `codigo_caja` M00-#### de una mesa inexistente. (3) La cobertura real del módulo es 17.04% y los seis comandos están literalmente al 0%: las 48 pruebas sólo tocan estaciones.ts, transiciones.ts y la forma de esquemas.ts, así que ninguno de los dos bloqueantes podría ser detectado jamás por la suite entregada. El diseño puro (tabla de transiciones, reparto por área/estación, resolución de estación) está bien hecho y bien probado; lo que falta probado es exactamente lo que escribe dinero y estados.
