import { Clientas } from '~/estetica-salon/Clientas';

/**
 * La ficha y el expediente: lo que hay que saber antes de tocar a alguien.
 *
 * La página es una línea a propósito — el componente vive en `apps/web/src/`,
 * donde el verificador de primitivas sí vigila los literales.
 */
export const dynamic = 'force-dynamic';

export default function Pagina() {
  return <Clientas />;
}
