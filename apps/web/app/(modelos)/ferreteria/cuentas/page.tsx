import { Cuentas } from '~/ferreteria/Cuentas';

/** La cartera de crédito de la ferretería: quién debe y a quién hablarle hoy. */
export const dynamic = 'force-dynamic';

export default function Pagina() {
  return <Cuentas />;
}
