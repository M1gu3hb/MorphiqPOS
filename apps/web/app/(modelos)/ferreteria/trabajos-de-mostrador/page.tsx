import { TrabajosDeMostrador } from '~/ferreteria/TrabajosDeMostrador';

/**
 * Apartados, listas del albanil y garantias: el clavo de los papeles.
 *
 * La página es una línea a propósito — el componente vive en `apps/web/src/`,
 * donde el verificador de primitivas sí vigila los literales.
 */
export const dynamic = 'force-dynamic';

export default function Pagina() {
  return <TrabajosDeMostrador />;
}
