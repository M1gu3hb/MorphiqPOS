import { Caja } from '~/ferreteria/Caja';

/** Las notas de mostrador esperando cobro, y el cobro mismo. */
export const dynamic = 'force-dynamic';

export default function Pagina() {
  return <Caja />;
}
