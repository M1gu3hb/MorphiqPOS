import { Registros } from '~/restaurante/Registros';

/** Registros: el archivo del negocio, para el dueño y el administrador. */
export const dynamic = 'force-dynamic';

export default function Pagina() {
  return <Registros />;
}
