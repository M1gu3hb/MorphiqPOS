import { Fiado } from '~/abarrotes/Fiado';

/**
 * La cartera de fiado de la tiendita, para el dueño y el encargado: a quién
 * hay que cobrarle y a quién ya no fiarle.
 */
export const dynamic = 'force-dynamic';

export default function Pagina() {
  return <Fiado />;
}
