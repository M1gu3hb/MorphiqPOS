import { PantallaEnrolar } from '@/identidad/PantallaEnrolar';

/** `/enrolar` — alta de la terminal, una sola vez (F1.1-A-02). */
export default function PaginaEnrolar() {
  return <PantallaEnrolar />;
}
