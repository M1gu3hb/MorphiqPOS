{
  "name": "@morphiqpos/data",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts",
    "./migraciones": "./src/migraciones/ejecutor.ts",
    "./catalogo": "./src/repos/catalogo.ts",
    "./stock": "./src/repos/stock.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "migrate": "node --conditions=react-server bin/migrar.mjs",
    "ensayo": "node --conditions=react-server bin/migrar.mjs --ensayo",
    "tipos": "node --conditions=react-server bin/generar-tipos.mjs",
    "bootstrap": "node --conditions=react-server bin/bootstrap.mjs"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "@types/pg": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:",
    "dotenv": "catalog:"
  },
  "description": "El unico lugar del monorepo que conoce SQL. Pool, transacciones, repositorios y migraciones.",
  "dependencies": {
    "@morphiqpos/contracts": "workspace:*",
    "@morphiqpos/domain": "workspace:*",
    "kysely": "catalog:",
    "pg": "catalog:",
    "server-only": "^0.0.1"
  }
}
